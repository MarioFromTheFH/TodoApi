{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker Compose Restart",
            "type": "shell",
            "command": "docker compose down && docker compose up --build",
            "presentation": {
                "reveal": "always",
                "panel": "new"
            },
            "group": "build"
        }
    ]
}